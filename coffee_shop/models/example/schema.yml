
version: 2

models:
  - name: customers
    description: "My first model for the AEC course"
    columns:
      - name: id
        description: "The primary key for this table"
        data_tests:
          - unique
          - not_null

